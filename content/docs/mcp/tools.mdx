---
title: MCP tools
description: Reference for every tool exposed by thesignup's MCP server.
---

Tools mirror the REST API one-to-one. Each tool accepts the same parameters as the corresponding endpoint and returns the same response shape.

> TODO: Auto-generate this page from the MCP server's tool manifest (or its OpenAPI projection).
