---
title: TypeScript SDK
description: Typed client for thesignup's REST API.
---

The official TypeScript SDK ships as `@thesignup/sdk` on npm.

> TODO: Link to the GitHub repo once the SDK ships. Add install + first-call examples.

```ts
// Placeholder — SDK not yet published.
import { TheSignup } from '@thesignup/sdk';

const client = new TheSignup({ apiKey: process.env.SIGNUP_API_KEY });
```
