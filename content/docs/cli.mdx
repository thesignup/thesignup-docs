---
title: CLI
description: Manage thesignup from the terminal.
---

The `signup` CLI wraps the REST API for scripting and local workflows.

> TODO: Document install, auth, and the most common commands once the CLI ships.

```sh
# Placeholder
brew install thesignup/tap/signup
signup auth login
signup orgs list
```
